---
description: 
globs: 
alwaysApply: true
---
# Webhook 模块结构说明

**入口与启动**
- 主入口为 `__main__.py`，负责日志配置、应用创建与启动。
- 应用通过 `app.py` 创建，注册所有路由、初始化数据库、扩展等。
- 使用 FastAPI 框架，支持自动生成的 OpenAPI 文档（访问 `/docs` 或 `/redoc`）。

**配置**
- 所有配置集中在 `config.py`，支持环境变量和 .env 文件，包含端口、调试、数据库路径等。
- 配置项应该通过 `pydantic.BaseSettings` 进行类型验证。

**路由与控制器**
- 路由通过 FastAPI 的 `APIRouter` 组织，每个子 router 在各自控制器文件顶部声明。
- 主要控制器文件：
  - `webhook_controller.py`：Webhook 相关接口
  - `project_controller.py`：项目配置接口
  - `task_controller.py`：任务相关接口
  - `third_party_controller.py`：第三方配置接口
  - `auth_controller.py`：认证接口
  - `events_controller.py`：事件接口
  - `metadata_controller.py`：元数据接口

**FastAPI 规范**
- 所有接口必须使用 `response_model` 或 `responses` 声明响应模型
- 请求体必须创建对应的 Pydantic 模型
- 所有模型定义应放在 `types.py` 中
- 使用依赖注入系统管理数据库连接和其他依赖
- 示例：
  ```python
  @router.post(
      "/webhook",
      response_model=WebhookResponse,
      responses={
          200: {"description": "Success"},
          400: {"description": "Bad Request"},
      }
  )
  async def webhook(
      event: GitLabPushEventModel,
      request: Request,
      db: sqlite3.Connection = Depends(get_db)
  ):
      ...
  ```

**数据库与模型**
- 数据库为 SQLite，文件为 `webhook_server.db`。
- 数据库初始化与表结构定义在 `models/database.py`。
- 主要模型文件：
  - `task.py`
  - `webhook_request.py`
  - `metadata.py`
  - `project.py`
  - `third_party.py`
- 数据库访问规范：
  - 所有需要数据库操作的接口必须通过 `db=Depends(get_db)` 显式传递数据库连接
  - 模型方法必须接收 `db` 参数
  - 示例：
    ```python
    @router.get("/tasks/{task_id}")
    async def get_task(
        task_id: str,
        db: sqlite3.Connection = Depends(get_db)
    ):
        return Task.get_by_id(task_id, db)
    ```

**扩展与中间件**
- 扩展位于 `extensions/` 目录：
  - `context.py`：数据库连接管理
  - `sse.py`：Server-Sent Events 支持
- 中间件应通过 FastAPI 的 `app.middleware()` 注册

**服务层**
- 业务逻辑服务位于 `services/` 目录，按功能划分
- 服务层方法应接收数据库连接作为参数
- 避免在服务层直接创建数据库连接

**工具与辅助**
- 工具函数位于 `utils/` 目录
- 通用类型和模型定义在 `types.py` 中
- 工具函数应保持纯函数特性，避免副作用

**类型和模型规范**
- 所有请求和响应模型必须使用 Pydantic 模型
- 模型应放在 `types.py` 中，按功能模块组织
- 模型命名规范：
  - 请求模型：`{Resource}Req`
  - 响应模型：`{Resource}Resp`
  - 数据模型：`{Resource}Model`
- 示例：
  ```python
  class GitLabPushEventModel(BaseModel):
      commits: List[Commit]
      ...

  class WebhookResponse(BaseModel):
      message: str
      task_id: Optional[str]
      ...
  ```

**前端资源**
- 模板文件位于 `templates/`，主页面为 `index.html`。
- 静态资源位于 `static/`：
  - JS 文件在 `static/js/`
  - CSS 文件在 `static/css/`
  - 字体等资源在 `static/css/fonts/`
- 不允许修改 taskClickHandler 代码段的内容！

**注意事项**
1. 数据库操作：
   - 始终通过依赖注入获取数据库连接
   - 避免在模型或服务中直接创建数据库连接

2. 类型安全：
   - 使用 Pydantic 模型进行请求和响应验证
   - 为所有接口定义明确的响应模型
   - 使用类型注解提高代码可维护性

3. 错误处理：
   - 使用 FastAPI 的异常处理机制
   - 定义清晰的错误响应模型
   - 统一错误处理方式

4. 性能优化：
   - 合理使用异步操作
   - 避免不必要的数据库查询
   - 使用适当的缓存策略

5. 安全性：
   - 实现适当的认证和授权机制
   - 验证所有输入数据
   - 保护敏感信息